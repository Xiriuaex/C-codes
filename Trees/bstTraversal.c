/*
    Tree traversal: Process of visiting each node in the tree exactly
                    once in some order.

        Breadth first:
            Level order:
                *we will use queue to track the children nodes.
                TC= O(n)
                SC= O(1) best case
                    O(n) worst case
                    
        Depth first:
            Preorder
            Postorder
            Indorder
*/

void LevelOrder(){

}